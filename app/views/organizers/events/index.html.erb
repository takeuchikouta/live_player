<div class="container py-5">
  <%= render 'searches/form' %>

  <h2>イベント一覧</h2>
  <ul>
    <% @events.each do |e| %>
      <li><%= link_to e.name, organizers_event_path(e) %></li>
    <% end %>
  </ul>
</div>